# Core runtime dependencies (mirrors pyproject.toml [project.dependencies])
# For Selenium/Google support: pip install better-bing-image-downloader[google]
requests>=2.28.0
tqdm>=4.60.0
filetype>=1.2.0
brotli>=1.0.0
